# Ledger demo transformation — build-ready spec

**Date:** 2026-07-06. **Provenance:** 11-agent workflow (2 ground readers, 5 expert seats, synthesis, 3 adversarial auditors), founder-commissioned. The stepper height-bounce fix already shipped separately (grid-stack, EN+ES).

## ⚠ VERDICT AMENDMENTS — binding corrections before any build item ships

The adversarial pass confirmed the spec is well-grounded but surfaced findings that BIND the build. The full verdicts are appended at the bottom; the must-fix set:

### HONESTY

- **[critical]** The demo's climax contradicts the rule it hands off to: the canned flag never fires under the actual shared rule. tools/_shared/bench-lookup.js computePriceHike is trailing-median + DUAL delta — deltaPct >= 8% AND deltaCents >= 500 ($5.00 floor, line 56). The spec's G4 set (median $24.35, Jun 26 $27.90) yields delta $3.55 = 14.6%: it clears 8% but fails the $5.00 floor. I executed the module: computePriceHike returns null for Jun 26, Jun 12, Jun 19, and also for the currently shipped 3-row set. P0-1's own AC ('local verification that bench-lookup.js's rule flags Jun 26') is unsatisfiable with these numbers. Worse, the spec's step-4 copy explicitly invites skeptics to replay the numbers in Vendor Benchmark ('same flag rule … mirrored to the cent'); a replay of $24.35 → $27.90 returns tier 'watch' with the copy 'Below the 8% / $5 line Bench flags as a hike' — the verify door publicly falsifies the demo's flag.
  - Fix: Re-center the seven history points so the trailing median is comfortably ≤ $22.90 while keeping Jun 26 at $27.90 (e.g., median $22.75 → delta $5.15 = 22.6%, clears both gates; a Jun 19 point near the median stays quiet). This means the Jun 12/Jun 19 rows in steps 2–3 ($24.10/$24.35) must change too — the spec's 'numbers unchanged' freeze covers only the step-0 invoice lines and $114.25, which survive. Recompute every derived number: bracket label ($5.15 a case), plate-cost line ($5.15 ÷ 24 ≈ 21¢ a head), and the band labels. Keep P0-1's verification AC — it is the right gate; the data must satisfy it before anything else in P0 ships.
- **[high]** The step-0 DevTools challenge is false as worded and will be falsified by the exact audience it invites. 'the only request is the cookieless, self-hosted page-count we publish' + 'no third parties': the shipped page (ledger/demo/index.html) fires fetch('/api/auth/me') and conditionally '/api/workbench/count' (nav auth, lines 477–495), p.js engagement beacons, four footer JS files, site.js, theme-toggle.js, fonts/CSS — and, decisively, loads https://challenges.cloudflare.com/turnstile/v0/api.js (a genuine third-party request) as soon as the footer newsletter form scrolls within 400px or receives focus (lines 958–975). The spec scoped the challenge only against the /api/event beacon; both halves of the claim fail the test on a normal scroll-through.
  - Fix: Reword the challenge to what is literally true, e.g.: 'no uploads — nothing you do in this demo leaves the page; the only analytics call is the cookieless, self-hosted page count, and the rest of the traffic is this site's own assets.' If 'no third parties' must survive, additionally remove the footer newsletter form (and thus Turnstile) from this page as part of the P1-8 leak-plug item, and note the /api/auth/me sign-in check or defer it on this path.
- **[medium]** The step-4 verify-door copy misstates Vendor Benchmark's baseline. Spec ruling #2 and §4: 'same flag rule … run against the public Cost Index instead of your own history' / 'a different baseline than your own history, on purpose.' Per tools/vendor-benchmark/index.html (lines 588–590), VB's hike flag runs against the trailing median of the user's OWN entered prices — 'the same rule Muntin Ledger uses, so the two never disagree' — while the Cost Index supplies a separate market-move read over the date window. The flag rule is never run 'against the Cost Index.' The spec claims to fix the shipped 'same read' looseness and instead codifies a new inaccuracy about a live, checkable tool.
  - Fix: Truthful sentence: 'Vendor Benchmark runs the same own-history flag rule Ledger uses — mirrored to the cent — and sets a market read from the public Cost Index beside it over your exact dates.' Apply to the ruling, the step-4 CURIOUS beat, and the peak micro-ask framing, in both locales ('la misma lectura' at es/ledger/demo/index.html line 571 is the ES target).
- **[medium]** Cross-step count contradiction the spec's own invariant forbids: step 2 will read '3 of 7 lines match' (the ledger = 7 lines total, 3 romaine), while step 3's new G4 chart displays 7 dated historical romaine purchases (Jan 15 … Jun 19) plus Jun 26 — 8 romaine lines in the same ledger. A reader inspecting across panels ('every visible number must sum, count, and subtract correctly on inspection') finds the search claiming 3 romaine matches in a ledger the chart proves holds 8.
  - Fix: Scope the count line to what is on screen — e.g. '3 of the 7 lines shown match' / '3 de las 7 líneas en pantalla coinciden' — or scope the search to a June window in the strip's printed text ('Search: romaine · June'). Keep the 7 visible rows; just stop implying they are the whole ledger.
- **[medium]** P0-1's registration AC rests on a false premise and misuses the fact registry. 'register in data/sourced-claims.json as illustrative, same regime as the invoice': the shipped invoice numbers are NOT registered in sourced-claims.json (verified — no such entries; only ledger_founding_offer_2026 is ledger-related). They are handled under docs/fact-check.md pattern 3 (labeled illustrative via chip + figcaption). The registry is pattern 1 — verified claims with source_url and date_verified; parking invented illustrative numbers there pollutes the system-of-record that ALLOWED_DEEP_LINKS and the gates trust.
  - Fix: Drop the sourced-claims.json registration from P0-1. The chip + figcaption 'illustrative' labeling the spec already mandates on every figure IS the regime the invoice actually uses, and it satisfies the fact gate. Keep the arithmetic self-check and the bench-lookup verification ACs.
- **[low]** G3's CSV figcaption 'the export, exactly as it leaves' pins an unverifiable format of an unreleased product. Nothing in this repo defines Ledger's export header as date,vendor,item,qty,price; Ledger is pre-GA (private beta per the registered claim). 'Exactly' is a falsifiable-later precision claim of the kind the site's honesty posture avoids.
  - Fix: Either verify the header against the actual product export before shipping and keep the sentence, or soften to 'a CSV export like this one — part of the mockup, not a live control.' The second half of the spec's caption already carries the mockup disclosure; drop 'exactly.'
- **[low]** The demo narrates band step-out as the flag rule; the product's rule is median + 8%/$5 co-gate. Step 3's copy and the bracket ('steps out of its six-month band — that is the flag', 'above your median — flagged') present the band as the mechanism, but no product surface describes a band rule — and the skeptic the spec routes to Vendor Benchmark will immediately see the real '8% / $5 line' named in VB's talking points, making the band story read as a misrepresentation of the rule rather than a visualization.
  - Fix: Keep the band as pure visualization but let one line of step-3 copy or the chart <desc> name the actual rule ('flagged because the move clears both of Ledger's thresholds — at least 8% and at least $5 over your trailing median'), so the demo and the verify tool describe the same mechanism in the same terms.
- **[low]** The 'no email capture on this page' global invariant is already false via site chrome: the shipped footer carries the newsletter email form (lines 800–818) on this very page, directly under the closing cap 'this demo takes nothing, not even an email.' Defensible as scoped to the demo surface, but it is the adjacency an adversarial reader will screenshot — and the Turnstile third-party load (finding 2) rides on that form.
  - Fix: Either suppress the footer newsletter block on both demo locales as part of P1-8 (which also cures the Turnstile leg of finding 2), or explicitly scope the invariant in the spec to the demo surface so the engineer doesn't certify an AC the page can't meet.
- **[low]** P1-8 asserts 'suppress the promo banner on this page' but no suppression mechanism exists: the data-hide-sticky-bar mechanism (line 900) covers only the mobile CTA bar, and grep finds no per-page switch for the batch-banner sentinels — inject scripts can re-add a manually deleted block on the next build.
  - Fix: Specify the mechanism: add a per-page opt-out the banner inject script respects (e.g., a data attribute or an exclusion list in the script), applied to both locale files, rather than a one-time manual deletion.

> Overall: The spec is unusually honesty-literate — the founding terms match the registered claim (data/sourced-claims.json#ledger_founding_offer_2026) verbatim in substance; the weeks-out sentinel exists and is genuinely machine-computed (scripts/build-site-counts.mjs:100 computes from Nov 13 2026); urgency is date+rate only with no scarcity theater; the three-stance strip (no rake, no-LLM build gate, export/delete) is backed word-for-word by /never/ and /ledger/ (lines 602, 609–611); romaine is in the CC0 Cost Index basket and the basket-scope sentence mirrors /ledger/'s scope paragraph; all cited line numbers in ledger/demo/index.html resolve correctly; p.js supports the proposed class-based tagged events cookielessly; no proposed copy trips scripts/lib/fabrication-patterns.mjs; every figure keeps an illustrative chip; and the rejected list correctly bars invented volumes and savings numbers. But it fails its own two centerpiece tests. First, the climax is arithmetically honest yet mechanically false: I ran tools/_shared/bench-lookup.js against the spec's canned series and the flag returns null — $3.55 fails the $5.00 half of the dual-delta co-gate — so the P0-1 verification AC cannot pass, and the spec's own skeptic-routing to Vendor Benchmark ('mirrored to the cent') guarantees public falsification. Second, the DevTools challenge is disproven by the page's real network behavior, including a genuine third-party Turnstile load from the footer it forgot to scope. Both are exactly the failure class the spec exists to prevent ('the demo never contradicts the tool it hands off to'). Fix the canned history (median ≤ ~$22.90 against the frozen $27.90) and reword the challenge before any P0 item ships; the medium/low findings are one-line copy and scoping corrections. Verdict: sound architecture, two load-bearing claims that verification defeats — not shippable as written, readily fixable.

### feasibility

- **[critical]** The canned data cannot fire the flag rule — the step-3 climax is unbuildable as ruled. Executed tools/_shared/bench-lookup.js (the module P0-1's AC names): computePriceHike requires BOTH deltaPct >= 8% AND deltaCents >= 500 ($5.00 absolute floor, DEFAULT_THRESHOLD_CENTS=500, line 56). Spec's data: median $24.35, Jun 26 $27.90 -> delta 355 cents / 14.6% -> returns null; assess() returns tier 'watch', not 'hike'. Minimum firing price against the $24.35 median is $29.35 (verified: $29.30 -> null, $29.35 -> fires). Three of the spec's own rulings are therefore mutually unsatisfiable: conflict-ruling #3 (median exactly $24.35), G1's 'the five lines and $114.25 total stay exactly as shipped', and §4's binding copy 'the same flag rule Ledger ships — mirrored to the cent'. Worse, the skeptic's verify move (step 4) actively backfires: an operator who enters the demo's own numbers into Vendor Benchmark gets 'watch', directly contradicting the demo — the exact failure mode the spec's honesty rules exist to prevent. P0-1's AC ('verify the set fires on Jun 26') fails by construction.
  - Fix: Re-open the numbers ruling BEFORE any build starts, because it cascades into P0-2's SVG labels, P0-3's bracket copy, P0-4's plate-cost line, and both locales. Option A (recommended): raise Jun 26 romaine to $29.40 (delta $5.05 / 20.7% — clears both gates with margin); invoice total becomes $115.75; plate-cost line becomes '$5.05 ÷ 24 heads ≈ 21¢'; drop G1's numbers-frozen invariant. Option B: keep $27.90 and lower history so median <= $22.90 (delta >= $5.00), abandoning ruling #3's $24.35 median. Option C (rule-parity copy removed) violates the spec's own honesty rules — reject. Effort: the decision plus ~0.5 day of mechanical renumbering across ~40 instances in 2 files, but it must land first.
- **[critical]** The step-0 'verifiable challenge' figcaption is false on this page and will be disproven by the first visitor who takes it. Verified in /home/user/potentially-profitable/ledger/demo/index.html: (a) the nav auth toggle fetches /api/auth/me on idle on every load (line 477) and /api/workbench/count when signed in; (b) p.js sends multiple /api/event beacons (pageview + engagement pings on visibility/scroll — not just one 'page-count'); (c) four footer scripts + site.js + theme-toggle.js load; (d) worst, Turnstile loads from challenges.cloudflare.com — a third party — once the footer newsletter form comes within 400px of the viewport (IntersectionObserver, line ~961), which happens on any full read of the page. 'The only request is the cookieless, self-hosted page-count' and 'no third parties' are both untrue; the spec's parenthetical scoped the claim only against the /api/event beacon.
  - Fix: Rewrite the challenge to what is actually true and checkable: 'watch the Network tab while you click through the five steps — the demo itself fires no requests; nothing you see here leaves your browser' (the stepper is pure DOM, verified). Qualify analytics separately ('the page's only analytics is our self-hosted, cookieless counter'). Do NOT promise an empty Network tab unless you also strip Turnstile/auth from this page — that fights the sync-includes canonical footer and is not worth it. Also reconcile the closing 'takes nothing, not even an email' with the footer newsletter email form that ships on this same URL.
- **[high]** G4 as written fails check-svg-dimensions.mjs (runs in check-all.mjs, line 354): every <svg> with a viewBox in page HTML must carry explicit width AND height attributes (pre-CSS 300x150 flash guard). The spec specifies viewBox + aspect-ratio boxes only, for all four new chart SVGs (2 variants x 2 locales).
  - Fix: Add width="640" height="300" and width="320" height="240" to the respective variants (EN + ES). Trivial, but a guaranteed CI failure if built per spec; add it to P0-2's AC.
- **[high]** Audit-premise corrections on the gate roster — two named gates don't exist or don't behave as assumed, and the gates that WILL bite went unmentioned. (1) There is no check-keyframes-allowlist.mjs anywhere in scripts/; the real gate is check-cls-animation.mjs, which scans only @keyframes bodies for layout properties (top/left/width/height/margin/padding/inset) — the spec's transition-only motion passes trivially, and even an opacity/transform @keyframes would pass with no allowlisting needed. (2) check-contrast.mjs and check-dark-contrast.mjs validate token VALUES in assets/site.css only — they cannot see page-level choices, so nothing gates the .45-opacity ghost rows (effective contrast of stone text at .45 is far below AA) or the .12 rust halo. (3) Unmentioned real hazards: check-svg-dimensions (above), check-all's inject-* idempotency runs (below), check-analytics-vocabulary --check (fail-CI on unregistered window.plausible() names).
  - Fix: Rewrite the spec's gate-compliance section against the actual roster in scripts/check-all.mjs. For the ghost rows: keep them, but bind a rule that ghosted rows never carry information absent elsewhere (they duplicate step-0 lines, so current design is acceptable) and note the accessibility judgment is unguarded by CI — it's on the reviewer.
- **[high]** P1-8 banner suppression fights scripts/inject-batch-banner.mjs and the CI idempotency run. The injector walks every HTML page with the canonical nav and re-stamps the <!-- batch-banner:start/end --> sentinel with the current batch banner on every run; it has no per-page opt-out; check-all runs inject scripts in --check mode, so hand-deleting the banner block from /ledger/demo/ fails CI on the next build ('1 page(s) would update').
  - Fix: Cheapest compliant path: page-scoped CSS in the demo's inline <style> — .batch-marquee{display:none} — leaves the sentinel intact and idempotent (the --banner-h measuring script reads offsetHeight 0, harmless). Alternative: add an opt-out (e.g. data-no-batch-banner marker) to the shared injector — more code, touches a shared script, only worth it if more one-ask pages are coming. data-hide-sticky-bar is confirmed real (footer script checks body.dataset.hideStickyBar, line ~900) and works as specced in both locales.
- **[high]** P0-7's acceptance criterion is nearly vacuous and the ES workload is understated. check-locale-parity.mjs checks only file existence and git-mtime staleness, and even --check mode is explicitly warn-only ('exit(0) during initial rollout', line 234) — it cannot verify structural parity of the chart, counts, or CTA order. Meanwhile the ES twin (/home/user/potentially-profitable/es/ledger/demo/index.html) is a full structural mirror with its own copy of the stepper IIFE and all inline CSS, so every P0 item genuinely doubles — including two MORE hand-built SVG chart variants whose <text> labels ('YOUR SIX-MONTH BAND', '6-MO BAND', 'above your median — flagged') must be translated into longer Spanish strings inside fixed 640x300 / 320x240 geometry ('BANDA DE 6 MESES' vs '6-MO BAND' is 2.4x wider), plus translated <title>/<desc>/data-audio-alt.
  - Fix: Treat ES parity as a manual checklist item with its own review pass, not a gate outcome. Budget SVG label widths for Spanish from the start (measure the ES strings first, or use shorter registered ES labels). Ship EN and ES in the same commit so the mtime-staleness heuristic stays quiet.
- **[medium]** G2's .lg-row grid change is a page-wide layout trap. The spec changes the shared .lg-row template to 6 tracks (52px 104px 1fr 34px max-content max-content) to restore the qty column, but the rows in steps 2 and 3 have only 5 children; CSS grid auto-placement would then put their prices in the 34px qty track and their status pills in the price column — silently breaking two other steps in both locales.
  - Fix: Scope the 6-track template to a modifier on step 1's container (.lg-rows--qty .lg-row{...}) or add a qty cell to every row in steps 2–3 (x2 locales). One-line decision now vs. a confusing visual regression later.
- **[medium]** Internal-consistency break between step 2 and step 3 under the spec's own 'every visible number must count correctly on inspection' invariant: step 2 says search 'romaine' matches 3 lines, but step 3's chart establishes 7 romaine purchases Jan–Jun 19 (+ Jun 26 = 8 romaine rows in the full ledger). A careful inspector — the exact reader this page courts — concludes either the search is broken or the ledger doesn't 'remember everything'.
  - Fix: Scope the visible list explicitly: make the ghosted 'date' facet chip the ACTIVE second facet (item: romaine · date: June) so '3 of 7' is true of the scoped view, or word the count line '3 of the 7 June lines match'. ES: mirror ('3 de las 7 líneas de junio').
- **[medium]** M1's row-tint overlay has a stacking-context bug as specced: .lg-row--flag::before{z-index:-1} on a row that creates no stacking context (position:relative alone doesn't) paints the tint BEHIND .lg-pane's opaque white background — the tint transition runs but is invisible. Same class of bug in both locales.
  - Fix: Add isolation:isolate (or position:relative;z-index:0) to .lg-row--flag so the ::before's z-index:-1 resolves inside the row's own context. One line; catch it in the spec so it doesn't ship as an invisible no-op.
- **[medium]** New inline CSS must carry pre-CSS fallback values or the paper invoice and chart flash unstyled: site-core.css loads async (preload + onload rel-swap, line 279), so during the no-CSS window tokens like --teal-tint, --elev-3, --ease-spring, --status-bad-tint, --line-dark are undefined. The existing demo CSS consistently writes var(--token,fallback) (e.g. var(--status-bad-tint,#F7E7DC), var(--rust,#C42E2E)); the spec's G1–G5 rules don't state the convention. Verified the token set itself is real and sufficient: --t-micro:120ms, --t-fast:180ms, --t-med:420ms, --ease-spring, --teal-tint:#EAF0FE, --elev-3 all exist in assets/site-core.css, and the dark remaps hold automatically (--teal-tint -> #1E2740, --rust -> #F0796A, --status-bad-tint -> #2E1A16 in the GEN:dark-mode block).
  - Fix: Add one binding line to §2: every var() in the demo's inline styles carries the light-mode literal as fallback, matching the file's existing convention. No gate enforces this; it's a review-checklist item.
- **[medium]** P0-2's four hand-built SVGs are the spec's biggest hidden maintenance cost for a one-person shop: ~30 hand-computed y()/x() coordinates per variant (8 points, band rect, bracket, gridlines, labels) x 4 SVGs, and finding #1 already forces one full renumber. Any future data change repeats the whole coordinate pass. Also two design pressure-points: the $24.10–$24.60 band is only ~26px tall in the 640x300 geometry (y-domain $23.50–$28.50), so the inside-left band label must be ~11–12px to fit; and the truncated y-axis is exactly the 'drama-zoom' the spec claims to avoid — the printed gridline labels are load-bearing, keep them in the AC.
  - Fix: Either write a ~40-line scripts/build-demo-chart.mjs generator (fits the repo's sentinel-driven build culture; regenerating on renumber becomes free) or, at minimum, embed the y() mapping formula and the data table as an HTML comment beside each SVG so a renumber is mechanical. Estimate P0-2 honestly at 1.5–2 days, not a peer of the other P0 items.
- **[low]** Analytics plan is buildable exactly as specced — verified the self-hosted assets/p.js parses class-based tagged events (plausible-event-name(=|--) and plausible-event-<prop> class matching present in the minified source), so P1-7 needs zero new JS. check-analytics-vocabulary --check (fail-CI) only scans window.plausible('…') call sites, so the class-based 'Demo Exit' can't trip it; check-event-prop-cardinality scans JS dirs only and the door= values are closed-enum literals.
  - Fix: Still register 'Demo Exit' in tools/_shared/analytics.js EVENTS to honor the registry-first discipline the gate's header documents; expect (and ignore) the informational 'registered but not fired' stale note, or add a one-line comment there explaining it fires via class-based tagging.
- **[low]** The ledger.ga_weeks_out sentinel is real and machine-computed — data/site-counts.json has ledger.ga_weeks_out: 19, build-site-counts.mjs line 100 computes it from Date.UTC(2026,10,13) - Date.now(), and inject-site-counts.mjs walks all HTML with nested-key lookup, so ledger/demo/ is in scope. But the value bakes at build time: the JSON says 19 (built 2026-07-02) while the true value today is ~18.6. On a static site the 'weeks out' number is only as fresh as the last deploy.
  - Fix: Acceptable at a weekly-or-better deploy cadence; add to P0-4's AC that build-site-counts + inject-site-counts run in the shipping build, and note the countdown's freshness dependency in the page comment.
- **[low]** Small correctness nits: (a) scroll-margin-top:84px ignores the batch banner (which sets --banner-h and sits above the 64px fixed nav) — deep links land under the banner when a batch is live; (b) P0-1's phrase 'register in sourced-claims.json as illustrative, same regime as the invoice' mis-states the current regime — the shipped invoice numbers are NOT registered anywhere in sourced-claims.json (verified: only 'claims' and 'operator_experience_claims' keys exist); they ride the fact gate's 'labeled illustrative in the prose' leg via the chips/figcaptions, and check-fabrications.mjs is a specific-pattern blocklist that the new numbers won't trip; (c) spec's line anchors are otherwise accurate (64, 79, 89–91, 144–150, 178, 687–693, 880 all check out), and the founding-terms sentence matches the registered ledger_founding_offer_2026 wording verbatim — keep quoting it whole as specced.
  - Fix: (a) scroll-margin-top:calc(84px + var(--banner-h,0px)); (b) either keep the chip-label regime (no registry change needed — simpler, matches precedent) or design a new illustrative_demo_data section in sourced-claims.json first (schema decision, don't improvise it inside P0-1); (c) no action.
- **[low]** P0 effort, realistically, for one engineer who is also the translator: P0-1 ~0.5d AFTER the renumber ruling (blocked until then); P0-2 1.5–2d (four SVGs, two languages, dark/360px passes, width/height attrs); P0-3 ~0.5d (CSS delay chain, ~10 JS lines mirroring the step-1 arming block, reduce-override extension at lines 91/178, stacking fix, no-JS/PRM/dark test matrix); P0-4 0.5–1d (copy x2 locales + the challenge rewrite, which needs a real network audit first); P0-5 ~0.5d (grid-template-areas restack — feasible pure-CSS without markup changes since date/vendor/status are already separate cells; ES pill wrap confirmed needed, '▲ el precio se movió' vs white-space:nowrap); P0-6 ~0.25d; P0-7 ~1d of translation + mirroring. Total ≈ 4.5–6 days — 'ships this week' only if the week contains nothing else and the P0-1 ruling lands on day one.
  - Fix: Re-sequence: (1) settle the renumber, (2) build the chart generator or coordinate comments, (3) EN build, (4) ES mirror as a distinct day, (5) run node scripts/check-all.mjs locally before calling any item done — the roster is ~100 gates and several unmentioned ones (svg-dimensions, inject idempotency, analytics vocab) are fail-CI.

> Overall: The spec is unusually well-grounded in the real file — its line anchors (64, 79, 89–91, 144–150, 178, 687–693, 880) all verify, the motion tokens it names all exist in assets/site-core.css, the ga_weeks_out sentinel and data-hide-sticky-bar mechanisms are real, and the self-hosted p.js genuinely supports class-based tagged events, so most of the plan is buildable with zero new dependencies as claimed. But it fails its own two most important promises. First and fatally: I ran the shared rule module the spec itself designates as the verifier — tools/_shared/bench-lookup.js computePriceHike requires >=8% AND >=$5.00, and the spec's climax delta is $3.55, so Jun 26 returns null and Vendor Benchmark's assess() returns 'watch'; the flag never fires, the 'same flag rule, mirrored to the cent' copy becomes a checkable falsehood, and the skeptic's verify door would let users disprove the demo with the demo's own numbers. Rulings #3 (median $24.35), G1 (invoice numbers frozen), and §4 (rule parity) cannot all hold; a renumber ruling must precede all P0 work. Second, the step-0 DevTools challenge is false on the live page (idle /api/auth/me fetch, multiple engagement beacons, and a genuine third-party Turnstile load from challenges.cloudflare.com when the footer form approaches). Beyond those: the audit premise's gate list is partly wrong (no check-keyframes-allowlist exists; check-cls-animation only polices @keyframes layout props, which the transition-only motion spec passes trivially; the contrast gates are token-level and can't fail on page choices), while the gates that WILL fail went unmentioned — check-svg-dimensions requires width/height attrs the G4 charts lack, and inject-batch-banner's CI idempotency run makes hand-deleting the promo banner a build failure (suppress via CSS instead). check-locale-parity is existence+mtime and warn-only, so P0-7's AC proves nothing; real ES parity — including two more hand-built Spanish SVGs in fixed geometry — is manual and roughly doubles the copy/markup work. Realistic P0 effort is 4.5–6 engineer-days for the one person who also translates, dominated by the four hand-coordinate SVG charts (recommend a tiny generator script, in keeping with the repo's sentinel-driven build culture, since finding #1 already forces one full renumber of every chart label). Verdict: green-light the architecture, block the spec on the P0-1 renumber and the challenge rewrite, and patch the six mechanical traps (SVG dims, grid scoping, stacking context, CSS fallbacks, banner suppression method, step-2/3 count consistency) before any code is written.

### Skeptical-operator value audit

- **[high]** HIGHEST-LEVERAGE GAP — the demo never closes the money loop. The only demonstrated value in five steps is one $3.55 case move (15 cents a head), and $19/mo is never priced against it. The spec rejected the chef's '$10.65/week' beat for fact-gate reasons (correctly — invented volume) but replaced it with nothing, so the operator is left to do the ROI math alone at the exact moment you ask for the email. Worse, the stake doesn't exist until step 3: steps 0-2 have zero forward pull, and a 5-click stepper bleeds users before the climax. The aha (total-hides-the-move) is real and well-built — but it's both unpriced and buried.
  - Fix: Make the $3.55 raise the demo's protagonist end to end, using only displayed values: (1) plant it at step 0 — one line under the invoice: 'One of these five lines is hiding a raise. The $114.25 total won't tell you which.' plus a visible 'Skip to the flag →' text link (the #ld-step3 deep link ships in P0-6 anyway); (2) cash it at the step-3 peak micro-ask: '$19 a month is less than six of these caught cases' (19 ÷ 3.55 — pure arithmetic on the displayed number and the posted price, passes the spec's own rejection rule). One planted sentence, one priced sentence, one skip link. This does more for conversion than every P1 item combined.
- **[high]** The 'verifiable challenge' fails its own verification. The step-0 figcaption dares the skeptic: 'open DevTools → Network... the only request is the cookieless, self-hosted page-count.' The spec scoped this against the /api/event beacon only — but the shipped page also fires /api/auth/me, loads four footer scripts, and lazy-loads Turnstile from challenges.cloudflare.com (a third party, line ~961) when the footer newsletter form nears the viewport. The one visitor who actually runs the test — your highest-value skeptic, the person the whole trust posture is built for — catches the page lying about 'no third parties.' Separately, DevTools-speak is developer persona, not chef-owner persona; it's proof theater aimed at the wrong reader on the wrong step.
  - Fix: Either make the claim true (audit every request the page makes with JS on, through footer scroll, and scope the sentence to what's actually observable) or soften it to operator language ('nothing you type or view leaves this page — auditable if you know your way around a browser') and move the technical challenge to the SKEPTICAL door in step 4, where the proof-literate audience actually self-selects.
- **[high]** Ingestion is never addressed — the demo's silent dodge. Step 1's stated feeling is 'relief: no retyping,' but nowhere in five steps does the page say how an invoice gets INTO Ledger (photo? forwarded email? PDF upload? manual entry?). The tired chef's actual nightly pain is the shoebox of paper; 'templates and rules pull the lines' begs the question of what he has to do first. A skeptic assumes the worst (manual upload of every invoice) and discounts everything downstream. Related: 'a build gate blocks anyone from adding one' is developer-insider language promoted UP the page by this spec — a chef doesn't know what a build gate is.
  - Fix: One clause in the step-1 body naming the real intake path — whatever it truthfully is. If the true answer is unflattering, the site's own honesty rules require saying it anyway; a demo that dodges it converts worse than one that answers it plainly. Translate the build-gate line into operator terms: 'no AI reads your invoices — and we've made it impossible for us to quietly change that.'
- **[medium]** WEAKEST ITEM 1 — P1-1/G1, the paper-invoice dressing (letterhead, dot leaders, -0.4deg rotation, double rule, rubber stamp). This is the spec's purest decoration-of-a-benefit: it spends craft on the INPUT — the one artifact the chef already stares at every day and needs zero help recognizing — at the moment of peak attention (first panel), while demonstrating nothing Ledger does. 'Recognition: that's my Tuesday' is a designer's story about the user, not a user need; the current plain card already reads as an invoice. Every hour here is an hour not spent on the ingestion clause or the ROI line, which live nowhere in the build list.
  - Fix: Demote P1-1 to P2 or cut it. Spend the step-0 slot on the planted stake and skip-link (see finding 1) — forward pull is what step 0 owes the funnel, not skeuomorphism. If it ships eventually, fine; it must not ship before the conviction gaps are closed.
- **[medium]** WEAKEST ITEM 2 — P1-3, step-2 'depth' (static facet chips, ghost rows, mark highlights). Search-your-purchases is table stakes — a spreadsheet does it, and the chef knows it — so decorating it harder doesn't make it more convincing. The fake facet-chip row ('captioned as mockup') adds inoperable UI to a page whose stated ethic is 'no live-looking inputs'; the spec even rejected a ghost Export button for exactly this reason (§6: 'looks operable'), then approved chips that have the same problem. The only load-bearing element in the whole step is the CSV <pre> — portability answers a real objection (lock-in).
  - Fix: Cut the facet chips. Keep <mark>, the reconciled '3 of 7' count (a genuine fix — the shipped page says '3 of 6' while showing 3 rows, which currently fails inspection), and the CSV snippet. Reframe the step's copy from 'search works' to the actual differentiator: 'this is the price spreadsheet you never had to build — it filed itself while you worked the pass.' That makes step 2 continue step 1's benefit instead of decorating a commodity feature.
- **[medium]** WEAKEST ITEM 3 — P1-4 + M3/M6 (directional panel entry, rail done-marking, :active button press-feel). Pure feel-polish, invisible or near-invisible to a tired operator on a phone, with zero conviction payload — the textbook case of 'more polish' mistaken for 'more convincing.' Adjacent tell: conflict ruling 6 keeps the step-1 scan by asserting it's 'mechanism, not filler' — but the new extraction-anatomy figure (which IS good) already carries the mechanism; the scan is choreography the committee talked itself into keeping. Nobody leaves an email because a panel slid 10px in the correct direction.
  - Fix: Push P1-4 and M6 to P2/never. Keep M1 (the flag landing genuinely serves comprehension — the sequence teaches the rule) and keep M4 (hash sync is correctness, not polish). If P1-2 runs long, ship the anatomy markup and drop the scan; the spec's own demo-craft seat wanted it demoted and was overruled on the weakest reasoning in the document.
- **[medium]** Step-4 heading now contradicts its own primary CTA. The resequence (Ledger first, correct call) keeps the title 'Now run your own line' — a promise of agency — above a primary button that asks for list commitment on a product that won't exist for 18 weeks. The one thing the heading promises is now the SECOND door. A skeptic reads that as bait-and-switch; a tired reader clicks the primary expecting a tool and lands on a pricing anchor.
  - Fix: Retitle the panel to match the new ask — 'Put this flag on every line' / 'Every invoice, the day it lands' — and let Vendor Benchmark own the 'run your own line' phrase at its door, where it's literally true. Also honestly answer 'why join today rather than October': if founding terms end at GA, say so (that's date-honest, not scarcity theater); if they don't, accept that the peak ask is structurally soft and let the Plausible door data confirm it.
- **[medium]** P1-8 is mispriorized: banner + sticky-bar suppression is the cheapest funnel protection in the whole document and it's sitting in P1 behind paper-invoice styling. Right now the shipped page runs a Father's Day promo banner (line 291) above the demo and a mobile sticky bar pushing 'Check prices' + 'Reach Don' — three competing CTAs on the page whose stated rule is 'one page, one ask.' Both suppressions are one attribute and one comment-out.
  - Fix: Move P1-8 into P0. It's minutes of work and it protects every dollar the rest of P0 earns. While there: verify the invoice is visible above the fold at 360x640 — banner + fixed nav + breadcrumb + eyebrow + display-size h1 + lede + five-pill rail is a lot of chrome before any product appears, and 'would you bounce' is answered right there for the phone visitor.
- **[low]** Disclaimer density borders on self-sabotage. Chip + figcaption + (new) rubber stamp per figure means the page's most-repeated message — eight to ten times across five panels — is 'this is not real.' The fact gate rightly requires the labels, and P1-10 varies the wording, but variation doesn't reduce volume; a skeptic's takeaway can become 'everything here is fake except the free tool.' The honesty posture is a genuine asset; the dosage needs managing, not the principle.
  - Fix: Let the chip carry the legal weight (it's the gate-satisfying label) and let figcaptions lead with the takeaway, ending in a three-word label ('Canned sample.') instead of a full disclaimer sentence. Fold this into P1-10's AC: four distinct wordings AND figcaptions that spend their first clause on meaning, not apology.
- **[low]** Patronization check — mostly clean, one hazard. The 15-cents-a-head line shows its division ($3.55 ÷ 24), which operators respect; the 'my guy or the market?' framing and 'withholds rather than guesses' restraint are the most operator-credible copy in the spec. The hazard: §4 names the step-4 beats READY / CURIOUS / SKEPTICAL. As internal beat names, fine; if any of that taxonomy leaks into visible copy ('for the skeptics...'), it names the reader's psychological state to their face, which is the one genuinely patronizing move available on this page. 'Don't take a canned demo's word for it' works precisely because it labels the DEMO, not the reader.
  - Fix: Add one line to P0-4's AC: audience-stance labels never appear in visible copy — doors are labeled by what they do (join / verify / audit), never by who the reader is.

> Overall: Verdict: this spec is roughly 70% conviction work and 30% self-indulgence, and — credit where due — the P0 list mostly knows the difference. The aha is REAL: 'the $114.25 total hides a $3.55 move' is a genuine operator insight, the band chart with printed gridlines is honest dataviz, VB-as-verify-door is a smart credibility move, and the step-3 micro-ask is correctly earned and correctly quiet. The committee's rejections (ghost export button, scarcity copy, invented volume, drama-zoom) show real discipline. But the spec optimizes the corridor to the aha instead of shortening it or pricing it. THE THREE WEAKEST ITEMS: (1) P1-1's paper-invoice theater — craft spent decorating the input the chef already knows, at peak attention, demonstrating nothing; (2) P1-3's facet-chip mockup row — decorating table-stakes search with inoperable fake UI the spec's own ethics rejected one section earlier; (3) P1-4/M3/M6 feel-polish — directional slides and button press-feel that no tired operator will ever perceive, plus ruling 6's 'mechanism, not filler' self-persuasion keeping a scan the anatomy figure already obsoletes. THE SINGLE HIGHEST-LEVERAGE CHANGE: make the $3.55 raise the demo's protagonist — plant it at step 0 ('one of these five lines is hiding a raise; the total won't tell you which', plus a Skip-to-the-flag link riding the P0-6 deep link), then cash it at the peak: '$19 a month is less than six of these caught cases' — pure arithmetic on displayed values, passing the spec's own fact rules. That closes the two real conversion holes (no forward pull before step 3, no bridge from flag to price) for two sentences and a link. Also non-negotiable before ship: fix the DevTools challenge, which currently fails its own test (Turnstile loads from challenges.cloudflare.com on this very page — the one skeptic who checks catches the 'no third parties' claim being false), answer the ingestion question somewhere in step 1, and pull P1-8's banner/sticky suppression into P0 — the page is running three competing CTAs today against its own 'one page, one ask' rule. Would I bounce? On a phone, possibly before the invoice even paints (banner + nav + breadcrumb + h1 + lede + rail), and probably at step 1→2 where nothing is yet at stake — both fixed by the same protagonist move. Do the two missing sentences before any of the P1 polish; they are the difference between a beautiful demo and a convincing one.


---

## THE SPEC (panel synthesis)

# Muntin Ledger demo — /ledger/demo/ build spec

**Scope.** One engineer, existing tokens (`assets/site-core.css`), zero new dependencies, zero per-visitor work. Every change ships to both files: `/home/user/potentially-profitable/ledger/demo/index.html` (EN) and `/home/user/potentially-profitable/es/ledger/demo/index.html` (ES) — the locale-parity gate requires it.

**Global invariants (binding on every item below).** Five steps, no more. No email capture on this page ("this demo takes nothing, not even an email" stays). No live-looking inputs. Canned data only, chip-labeled illustrative on every figure. The no-JS stacked page is always byte-complete. All motion is opacity/transform only, inside `@media (prefers-reduced-motion:no-preference)` (or via the existing reduce-override pattern at lines 91/178) plus the JS `prm` guard; base CSS is always the finished state. Every visible number must sum, count, and subtract correctly on inspection. The grid-stack fixed-height fix (`visibility:hidden`, lines 144–150) is untouchable.

## Conflicts resolved

1. **SVG line-draw:** motion seat proposed a `stroke-dashoffset` draw-in as a "sanctioned exception"; dataviz and chef seats ban paint-property animation. **Ruled: compositor-only wins.** The flag lands via staged opacity/transform on grouped SVG elements.
2. **Step-4 order:** all four seats that touched it agree — **Ledger first, Vendor Benchmark second as the verify path**, and "the same read" becomes "the same flag rule, on a different baseline."
3. **History length:** chef wanted ~26 weekly points, dataviz wanted 7. **Ruled: 7 points** (fewer registered numbers, readable at 360px, median still exactly $24.35).
4. **Mobile rows:** motion seat wanted keep-date/drop-vendor columns; dataviz wanted a two-line stack. **Ruled: two-line stack** — it preserves the date axis *and* vendor *and* fixes the ES pill overflow in one CSS change.
5. **Step 4 in the grid-stack:** chef wanted the CTA panel pulled out of the equalized stack. **Ruled: stays in.** Removing it rewrites the stepper contract (rail, "Step N of 5", loop) and risks the founder's reflow fix. The void shrinks via content growth in steps 0–3 plus the mobile controls reorder.
6. **Step-1 motion:** demo-craft wanted it demoted; motion seat wanted a reworked two-phase scan. **Ruled: keep, shortened** — once step 1 actually depicts extraction, a sub-550ms row-then-stamp scan is mechanism, not filler. The flag still owns the largest choreography.
7. **Steps 0–1 merge (chef):** rejected; five beats stay, but each gets a distinct job.

---

## 1. Narrative arc

Five beats, five feelings, each benefit **demonstrated**:

**Step 0 — "The invoice arrives." (recognition: "that's my Tuesday").** The user sees an actual-looking paper invoice — under-sheet, letterhead, dot leaders, double-rule total, a rubber-stamped "SAMPLE · ILLUSTRATIVE" — not a text card. The figcaption becomes the verifiable challenge (see §4). *Demonstrates:* the input is a real-world document; the honesty is designed in, not apologized for.

**Step 1 — "Read." (relief: "no retyping").** A dissected invoice line with teal field-tags (VENDOR / DATE / ITEM / QTY / PRICE) sits above the ledger rows — quantity column **restored** — and the rows scan in: row appears, then its "read" stamp commits. *Demonstrates:* deterministic template/rules extraction of vendor, item, quantity, price; the promoted note "rules, not a language model — a build gate blocks anyone from adding one" stays and moves up.

**Step 2 — "File." (trust: "it remembers everything, and I can leave with it").** The search strip narrows a visibly longer list: 3 romaine rows highlighted via `<mark>`, 4 non-matching Jun 26 lines ghosted at .45 opacity — the count line now reads **"3 of 7 lines match"** and is countable on screen. Static facet chips (item: romaine · vendor · date) and a two-line literal CSV `<pre>` snippet captioned "the export, exactly as it leaves." *Demonstrates:* search by item/vendor/date; CSV portability (verified features); no lock-in.

**Step 3 — "Flag." (vindication: the climax).** The full annotated six-month band chart (§2, G4). The rust step-out **lands** — segment, dot, bracket, pill, row tint, in sequence — the page's only large motion. Copy adds three short beats: (a) *the total hides the move* — the Jun 26 invoice totals an unremarkable $114.25 while romaine moved $3.55/case; caught the day it lands, not at month-close; (b) *plate-cost consequence* — "$3.55 ÷ 24 heads is about 15¢ a head; every dish romaine touches re-costs the same day"; (c) *my guy or the market?* — romaine is in the Cost Index basket, so the flag arrives with market context; outside the basket (spirits, wine, graded cuts) a raise still files and re-costs but isn't market-checked; on thin history the flag withholds rather than guesses. A quiet one-line micro-ask links the founding terms (§4). *Demonstrates:* own-history banding, dual-delta rule, same-day re-costing, vendor-vs-market discrimination, honest restraint.

**Step 4 — "Now run your own line." (agency).** Ledger-first ask with the `.lg-price-figure` lockup and full founding terms; Vendor Benchmark reframed as the skeptic's verify move ("same flag rule, public Cost Index baseline — a different baseline than your own history, on purpose"); a three-stance strip (no rake / no LLM, build-gated / export-and-delete anytime); three doors for three audiences. Calm, no motion. *Demonstrates:* posted pricing, the trust covenants, the proof-first funnel — honestly sequenced.

---

## 2. Visual system

All graphics inline HTML/CSS/SVG on existing tokens. Rust budget: rust appears **exactly twice** — the step-3 flag moment and its row pill. Honesty chips stay on every figure, reworded so the four disclaimers read differently (presence never varies, wording does).

**G1 — Paper invoice (step 0).** Activate the shipped `.lg-sheet` (line 64) behind the pane; pane gets ~-0.4deg rotation and `--elev-3`. Letterhead: "Produce vendor" Fraunces 18/600 left, "INVOICE" letterspaced uppercase `--stone` right; meta line "Jun 26 · Net 15 · 5 lines" tabular. Line items in vendor register — uppercase abbreviated names ("ROMAINE HRTS 24CT ×1") — with dot leaders (flex filler span, `border-bottom:1px dotted var(--line-dark)`) to right-aligned prices; total under a true double rule (1px + 3px `--ink`). **Numbers unchanged:** the five lines and $114.25 total stay exactly as shipped. The chip becomes a rubber stamp: 1.5px solid `--ink` border, uppercase, `rotate(-2deg)`, radius 4px, opacity .85 — ink, never rust. Single column; nothing breaks at 360px.

**G2 — Extraction anatomy (step 1).** Above the ledger rows, one dissected line in G1's register: `ROMAINE HRTS 24CT ×1 ……… $27.90`, with teal capture spans (`box-shadow:inset 0 -2px var(--teal)` + `--teal-tint` wash) under item, quantity, price; floating field tags (ITEM / QTY / PRICE, Inter 10.5px uppercase, `--teal`); VENDOR and DATE tags on a miniature header line. No connector SVG — proximity does the work. Below, the five ledger rows with the **quantity column restored**: `.lg-row` grid becomes `52px 104px 1fr 34px max-content max-content` at ≥561px. The uppercase paper register landing as "Romaine, 24 ct" rows makes the read visible. Stacked vertically on mobile.

**G3 — Filing pane (step 2).** Search strip loses `aria-hidden` (it's printed text, not fake UI); "Romaine" in matched rows wrapped in `<mark>` styled `background:var(--teal-tint);color:inherit`. Seven rows visible: Jun 26 romaine + Jun 12 + Jun 19 romaine (matches), four other Jun 26 lines ghosted (`opacity:.45`, compressed). Count line: "3 of 7 lines match." Facet chip row above (active `item: romaine`, ghost `vendor` / `date`), captioned as mockup. Bottom: `<pre>` CSV snippet — `date,vendor,item,qty,price` + one row — figcaption "the export, exactly as it leaves. Part of the mockup, not a live control." ES: "3 de 7 líneas coinciden."

**G4 — The money shot: six-month band chart (step 3).** Replaces the 280×44 SVG entirely.

*Canned data — register in `data/sourced-claims.json` as illustrative, same regime as the invoice:* Jan 15 $24.35 · Feb 12 $24.10 · Mar 12 $24.60 · Apr 9 $24.20 · May 14 $24.50 · Jun 12 $24.10 · Jun 19 $24.35. Sorted median = **$24.35**; band min–max = **$24.10–$24.60**; Jun 26 $27.90 = median + **$3.55**. Before shipping, verify the set fires on Jun 26 and stays quiet on Jun 12/19 under the actual shared rule (`tools/_shared/bench-lookup.js` trailing-median + dual-delta), so the demo never contradicts the tool it hands off to.

*Desktop variant* (viewBox `0 0 640 300`, `max-width:640px`): Y domain $23.50–$28.50, three hairline gridlines at $24/$26/$28 (`--line`, printed labels so the scale is auditable — no drama-zoom); band rect y($24.10)→y($24.60), `--teal-tint` at .55, dashed `--line-dark` edges, inside-left label "YOUR SIX-MONTH BAND · $24.10–$24.60"; `--stone` 2px polyline through the seven points, dots r=4 with 2px `--surface-1` rings; selective labels only (Jan $24.35, Jun 19 $24.35, Jun 26 $27.90 in Fraunces 20u `--ink`); the Jun 19→26 segment in `--rust` 2.5px, endpoint dot r=5.5 + white ring + static halo (r=11, rust .12); right-margin rust bracket y($24.35)→y($27.90) labeled "▲ $3.55 a case" with "above your median — flagged" beneath in `--ink-soft`; X baseline with ticks Jan · Mar · May · Jun 26. The flag pill lives in the row only, not duplicated in-SVG.

*Mobile variant* (viewBox `0 0 320 240`): two x-ticks (Jan · Jun 26), band label "6-MO BAND", delta stacked two lines, 12–13u type. Swap via `@media (max-width:560px)` display toggle; each in an `aspect-ratio` box (no CLS); hidden one `display:none` so AT sees exactly one.

*Accessibility:* remove `aria-hidden`; `role="img"` + `<title>/<desc>` speaking the actual numbers; `data-audio-alt` ≥80 chars on the figure; the three `.lg-row`s above remain the visible data table. Rust group wrapped in `<g class="lg-flag-group">` for the choreography.

**G5 — Price lockup (step 4).** Wire the shipped `.lg-price-figure` (lines 111–131) at reduced scale: $19 in Fraunces `clamp(48px,6vw,72px)`, "/month" per-style, and the *full* registered terms in the `.lg-price-ga` line: "Three months free for the founding list, then $19 a month for as long as you stay · opens November 13, 2026." Never $19 stripped of terms.

**Mobile row system (steps 1–3, ≤560px).** Replace the column-hiding at line 79 with a two-line stack: line 1 = item + qty + price (Fraunces number right); line 2 = `Jun 12 · Produce vendor · filed` in 11.5px `--stone`; `.lg-flag{white-space:normal;text-align:right;line-height:1.3}` — the Jun 12→19→26 progression survives on phones and the ES pill wraps.

**Hygiene.** Delete dead inline CSS this work doesn't consume (`.lg-hero`, `.studio-toc`, `.studio-grid/card`, `.ledger-asym`; `.lg-sheet` and `.lg-price-figure` are now used). `scroll-margin-top:84px` on `.ld-panel h2`.

---

## 3. Interaction & motion spec

Tokens: `--t-micro` 120ms, `--t-fast` 180ms, `--t-med` 420ms, `--ease`, `--ease-out`, `--ease-spring`. Contract: finished state is base CSS; animation only inside `prefers-reduced-motion:no-preference` + the JS `prm` guard; each choreography runs once (`dataset.played`); no loops, no idle motion; nothing animates on the CTA panel.

**M1 — Flag landing (step 3, ~960ms, the budget's owner).** Band + stone history render static always. On first arrival at step 3 (`i===3 && !prm && !played`): JS adds `lg-armed` to the vignette, then double-`requestAnimationFrame` adds `lg-landed` (~6 lines mirroring the step-1 arming block). All sequencing in CSS `transition-delay`: t=120ms rust segment group fades/rises in (opacity 0→1, translateY(-4px)→0, 240ms `--ease-out`); t=360ms dot pops (scale(.4)→1, 240ms `--ease-spring`, `transform-box:fill-box;transform-origin:center`); t=480ms bracket + label fade (240ms); t=600ms the existing pill drop (lines 89–90, add the delay) and the row tint via compositor-safe overlay — `.lg-row--flag::before{inset:0 -6px;background:var(--status-bad-tint);opacity:0;z-index:-1}` → `opacity:1`, 360ms. Extend the reduce override (line 91) to cover `.lg-flag-group` members and the `::before`; no-JS and reduced-motion see the finished chart instantly.

**M2 — Step-1 scan (shortened, re-sequenced).** Row: 200ms `--ease-out`, 60ms stagger via CSS `:nth-child` delays under an `.ld-go` class; stamp: `.lg-s` scale(.85)→1, 180ms `--ease-spring`, per-row delay +120ms. JS shrinks to two class adds (arm, double-rAF `.ld-go`); delete the setTimeout loop and 1500ms catch-all (lines 687–693). Total <550ms.

**M3 — Directional panel entry.** `show()` sets `panelsEl.dataset.dir = n<i?'back':'fwd'`; CSS animates only the incoming panel: `ld-enter` (10px rise+fade, `--t-med` `--ease-out`) / `ld-enter-back` (10px settle). `.ld-ready` added via double-rAF after initial `show(0,false)` so first paint never animates. Layout untouched.

**M4 — Navigation correctness.** (a) Boot: `var m=/^#ld-step([0-4])$/.exec(location.hash); show(m?+m[1]:0,false);`. (b) User nav writes `history.replaceState(null,'','#ld-step'+i)`. (c) Rail clicks bail before `preventDefault()` on `metaKey/ctrlKey/shiftKey/altKey/button!==0`. (d) ArrowLeft/ArrowRight `keydown` bound to the wrap only, clamped (loop stays on "Start over"). No swipe.

**M5 — Mobile frame.** At ≤640px under `.ld-js`: wrap becomes flex-column with `#ldControls{order:1}`, `#ldPanels{order:2}`, `#ldLive{order:3}` — Back/Next sit above the reserved-height void. Document the DOM/visual order divergence in a comment (acceptable: focus moves programmatically to the panel h2).

**M6 — Small state feel.** Rail visited-marking: `li.toggleAttribute('data-done',k<i)`; `.ld-rail li[data-done] a::before{color:var(--teal)}`; `--t-fast` transitions on pill border/background. `.ld-controls .btn:active{transform:translateY(1px) scale(.99)}`.

**M7 — Non-visual parity.** Step-3 `aria-live` announcement appends one payoff sentence: "The June 26 romaine line steps out of its six-month band — that is the flag." Search strip un-hidden (G3); every meaning-bearing figure carries `data-audio-alt` ≥80 chars; the chart's `<title>/<desc>` narrate the numbers.

---

## 4. CTA architecture (honest-conversion rules)

**Rules (binding):** urgency is date + rate only — the GA date, the machine-computed `<!-- count:ledger.ga_weeks_out -->` sentinel, and "three months free, then $19/mo for as long as they stay" (`sourced-claims.json → ledger_founding_offer_2026`), always quoted whole. No scarcity theater, no timers, no capture on this page, no interrupt patterns. Never "the same read" — the truthful sentence is "the same flag rule Ledger ships — mirrored to the cent — run against the public Cost Index instead of your own history."

**Peak micro-ask (step 3).** One quiet `.lg-cap`-styled line under the chart: "Ledger runs this check on every line, the day the invoice lands. Opens Nov 13, 2026 — [weeks-out sentinel] weeks out. The founding terms →" (text link → `/ledger/#pricing`). No button, no panel.

**Terminal panel (step 4), three beats.**
1. **READY:** "That flag, on every line of every invoice, the day it lands — Muntin Ledger opens November 13, 2026." Price lockup (G5). Primary light button "Join the founding list →" → `/ledger/#pricing`.
2. **CURIOUS (below the rule, named as the verify door):** "Don't take a canned demo's word for it. Vendor Benchmark runs the same flag rule on a price you've actually paid — against the public Cost Index, a different baseline than your own history, on purpose. Free, in your browser, no signup." Ghost button → `/tools/vendor-benchmark/`.
3. **SKEPTICAL:** compact text-link row "Check what we claim →" → `/trust/`, `/never/`, and the CC0 Cost Index download. Plus a terse three-stance strip, all verified: *No cut of sales, ever · No language model reads your invoices — build-gated, never training data · Export the whole ledger anytime in a format you can open without us; delete it and it's gone.*

Closing cap stays: "this demo takes nothing, not even an email."

**The verifiable challenge (step-0 figcaption rewrite).** "Canned sample, on purpose — no uploads, no third parties. Verify it: open DevTools → Network and click through; the only request is the cookieless, self-hosted page-count we publish." (Scoped so the challenge survives its own test against the `/api/event` beacon.)

**Measurement (aggregate only).** Class-based Plausible tagged events on the exit links: `plausible-event-name=Demo+Exit` with `door=founding|benchmark|proof|peak-founding`. No cookies, no new JS, no URL-param tracking. Decision rule: proof door dominates → invest in proof density; benchmark dominates → tighten the tool→Ledger return path.

**Leak plugs.** Set `data-hide-sticky-bar` on this page's `<body>` (mechanism exists, line 880) — keep "Reach Don" reachable via footer; suppress the promo banner on this page. One page, one ask.

---

## 5. Prioritized build list

### P0 — ships this week (the climax, the ask, and every number that must tie out)

**P0-1. Canned romaine history + reconciled counts** [copy]
AC: seven history points registered as illustrative in `data/sourced-claims.json`; sorted median = $24.35, band $24.10–$24.60, delta $3.55 — self-checks; local verification that `bench-lookup.js`'s rule flags Jun 26 and not Jun 12/19; step-2 count reads "3 of 7 lines match" (ES "3 de 7") and exactly 7 rows are visible; `check-fabrications.mjs` and `check-all.mjs` pass.

**P0-2. Step-3 band chart, both variants** [markup][copy]
AC: desktop 640×300 and mobile 320×240 variants per G4; media-swap with `aspect-ratio` boxes, zero CLS; gridline values printed; no second data series; `role="img"` + `<title>/<desc>` + figure `data-audio-alt` ≥80 chars; chip + figcaption retain the illustrative label; renders complete with JS disabled.

**P0-3. Flag-landing choreography** [css][js-minimal]
AC: per M1 — fires once on first arrival at step 3; opacity/transform only (grep confirms no `stroke-dashoffset`, no layout/paint properties in new rules); reduced-motion and no-JS show the finished figure with no hidden pill; JS delta ≤10 lines inside the existing IIFE.

**P0-4. Step-4 resequence + stance strip + baseline fix + peak micro-ask + challenge figcaption** [copy][markup]
AC: order = Ledger primary → VB verify → proof links, per §4; "same read" removed in EN and ES, replaced by same-rule/different-baseline phrasing; founding terms always the full registered sentence; weeks-out uses the count sentinel (never hand-typed), present in both locales; step-3 micro-ask is a text link, not a button; closing cap unchanged; DevTools challenge scoped per §4.

**P0-5. Mobile row stack + heading offset + ES pill** [css]
AC: at ≤560px each row shows item/qty/price plus a date·vendor·status second line — dates visible in steps 2–3; ES "▲ el precio se movió" wraps without overflow; `.ld-panel h2{scroll-margin-top:84px}`; no horizontal scroll at 360px.

**P0-6. Hash sync + modified-click guard** [js-minimal]
AC: `/ledger/demo/#ld-step3` opens step 3 with JS on; navigation `replaceState`s the hash (history length unchanged after 5 clicks); cmd/ctrl/middle-click on rail pills opens normally; no-JS anchors still work.

**P0-7. ES twin parity for all P0 items** [markup][copy]
AC: structural parity of chart, counts, CTA order, translated `<title>/<desc>`/`data-audio-alt`; `check-locale-parity.mjs` passes.

### P1 — next

**P1-1. Step-0 paper document** [markup][css] — AC: `.lg-sheet` visible under a rotated pane; dot leaders, double-rule total, ink stamp chip; the five lines and $114.25 unchanged; no rust; no images.

**P1-2. Step-1 extraction anatomy + scan rework** [markup][css][js-minimal] — AC: dissected line with five field tags; qty column restored at ≥561px; scan per M2 with sequencing in CSS delays; setTimeout loop and 1500ms catch-all removed; total motion <550ms; reduce/no-JS complete.

**P1-3. Step-2 depth** [markup][copy] — AC: `<mark>` highlights; four ghost rows at .45; facet chips captioned as mockup; `<pre>` CSV snippet whose row matches a visible ledger row; search strip no longer `aria-hidden`; figcaption keeps "not a live input."

**P1-4. Directional panel entry + rail done-marking + press feel** [css][js-minimal] — AC: per M3/M6; first paint never animates; incoming panel only; fixed height untouched.

**P1-5. Price lockup + dead-CSS removal** [css][markup] — AC: G5 wired with full terms; `.lg-hero`, `.studio-toc`, `.studio-grid/card`, `.ledger-asym` removed from this page with zero body occurrences confirmed.

**P1-6. Keyboard arrows** [js-minimal] — AC: per M4d; scoped to the wrap; clamped; no page-wide listener.

**P1-7. Plausible door tagging** [markup] — AC: four tagged links (three doors + peak micro-ask) using class-based events through the self-hosted proxy; no new script, no cookies.

**P1-8. Sticky bar + banner suppression** [markup][css] — AC: `data-hide-sticky-bar` set on both locales; promo banner suppressed on this page; footer "Reach Don" path intact.

**P1-9. Mobile controls reorder** [css] — AC: per M5 at ≤640px; DOM order unchanged; divergence documented in a comment.

**P1-10. Disclaimer variation pass** [copy] — AC: all four chips + figcaptions present; four distinct sentences; every figure still labeled illustrative; audio-alt coverage on all `lg-vignette` figures.

---

## 6. Rejected

- **`stroke-dashoffset` line-draw (motion P1a)** — paint-property animation; two seats ban it; staged opacity/transform achieves the landing.
- **"$10.65/week at three cases a week" (chef P1)** — requires an invented purchase volume; derived numbers must be arithmetic on *displayed* canned values only. The 15¢/head line stays (pure $3.55÷24).
- **~26-week weekly series (chef P1)** — 7 registered points make the band credible with fewer numbers and survive 360px.
- **Tax/fuel-surcharge line + varied quantities (chef P2/P7)** — destabilizes the $114.25 tie-out across three steps and two locales; fewer registered numbers is the safer fact posture.
- **Merging steps 0–1 (chef P2)** — five-beat rail, ES parity, and deep-link anchors are load-bearing; each step gets a distinct job instead.
- **Pulling step 4 out of the grid-stack (chef P5b)** — rewrites the stepper contract and risks the founder's reflow fix; M5 + content growth solve the void.
- **Ghost "Export CSV" button strip (dataviz P4d)** — looks operable; the `<pre>` snippet is the honest artifact.
- **Ghost chart in the CTA panel (dataviz P6a)** — the ask stays calm; the price lockup earns the panel's one visual slot; decorative rhyme isn't worth the ink-panel clutter.
- **Duplicate flag pill inside the SVG (dataviz G4 detail)** — one pill (the row's) + one bracket (the chart's); duplication doubles AT announcements and crowds the 320px variant.
- **Pane-elevation cross-fade (motion P6c)** — the seat itself called it skippable; panel entry masks the snap.
- **Plate-cost micro-figure (chef P4)** — one sentence carries it; a mini-dashboard drifts toward UI cosplay.
- **Invoice number / new paper-only numerals** — adds unregistered-looking specifics with no load-bearing job.
- **Any live search/upload affordance, swipe, auto-advance, scarcity copy, savings/adoption numbers, Cost Index overlay on the step-3 chart, lock-or-float or peer-price mentions** — barred by the fact gate, privacy posture, wholesale-honesty rules, or motion contract; no seat proposed them and none may enter in review.

*≈3,150 words.*
