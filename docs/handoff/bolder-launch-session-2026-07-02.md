# Handoff — bolder pass + strengths + launch session (2026-07-02)

_Branch: `claude/muntin-bolder-design-pass-o3ym41` (storefront). All 11 commits pushed; no PR opened (founder's rule). Written so a fresh context can resume in one read. Companion: `docs/design/decisions/ADR-002-bolder-pass-receipts-frame.md`, the updated `docs/handoff/strategic-council-board.md`, and the original brief `docs/handoff-bolder-pass.md` (constraints there still bind)._

## What shipped (chronological, all EN+ES, all gates at baseline)

1. `4511a574` **/ledger/ premium storefront** — goldenhour hero, illustrative printed-ledger vignette (DOM-text rows, generic "Produce vendor", triple illustrative labels, numeral-free step SVG; mechanism-not-UI because the product is in beta), ink pricing band with Fraunces $19 lockup, elevation+ink-frame cards, ruled numeral list, muntin waypoints. Founding offer registered: `sourced-claims.json#ledger_founding_offer_2026`.
2. `b375c012` **Homepage mid-page material** — stance panes (elev-1 + ink top frame + Fraunces numerals), instrument glyphs in flagship cards (numeral-free, labeled illustrative), dark closer de-drifted from blue radial glow to ink + muntin frame lines, window glyph on the definition section, tabular count chips.
3. `d9a912ce` **Tools hub** — cards on the elevation scale, walkaway line in the teal basis grammar, dead band closed, closer de-drifted. Page-inline only (generator untouched).
4. `2a7a1549` **Article shell** — takeaways/TLDR panels get elev-1 + ink frame via `assets/site.css` (shells REGENERATE from site.css via `@shell:` markers — never edit `site-core/tool/article.css` directly).
5. `b55bbe3e` **QA fixes** — tools cascade (inline blocks BEFORE the async CSS links lose ties; fix = doubled-class selectors), dark pricing band pinned to `#1B1E24`/`#2C3038`, rust fallbacks, vignette polish.
6. `<demo-closure>` **Demo closed** — founder: no own-invoice demoing yet. Zero links to `ledger.muntin.digital/demo` remain.
7. `6356da39` **"Receipts, not promises"** — studio-era stances replaced (3-draft panel → judge; Draft B receipts frame won). Panes: fact gate / network tab / public scorecard. Trust strip rebuilt. "Notes from the floor." New `claims.sourced` count sentinel; calibration figures pinned via `cal:` sentinels (`inject-cost-index-calibration.mjs` gained subset-target mode; `index.html` in TARGETS).
8. `853921af` **Footer trust column** — /claims/ + /status/ site-wide via `_includes/` partials + `sync-includes` (pipeline order: sync → `inject-site-counts`, partials carry stale count values by design).
9. `7a3f3eba` **Strength placements** — scorecard band on /cost-index/ hub via `scorecardBand()` in `build-cost-index-pages.mjs` (reads calibration report at build; EN+ES from one function). NOTE: hub regen also regenerates 350+ ingredient pages with unrelated generator drift — commit only the hub + generator, revert the rest (deploy regenerates). CC0 as an offer; 5-test audit invitation on tools hub.
10. `417f13e7` **Launch mechanics** — founding form ON /ledger/#pricing (`source=ledger-pricing`); homepage attribution `studio-hero`→`home-founding`; honest urgency ("before launch — now N weeks out" via new `ledger.ga_weeks_out` build count, floor of weeks to 2026-11-13); tools band routed on-site; margin-math stale studio footer fixed (tool-utility footers are SKIPPED by sync-includes — hand-edit those).
11. `560e66a5` **Design-crit batch** — site-wide nav clip fixed ("Reach Do" at 1100–1600px; progressive gap-shedding in @shell:core, placed BEFORE `GEN:dark-mode` because same-specificity later-in-file wins); vignette fixed columns; $19 at clamp(72,8vw,110); hanging stance numerals; founding band re-grounded on cream-2 + ink frame; dispatch banner static on phones; mobile heroes get full-strength goldenhour.
12. `dba314a0` **/ledger/demo/ + /es/ledger/demo/ — the mockup demo** — 5-panel guided walkthrough (invoice $114.25 → read by rules → filed searchable w/ display-only search → romaine flagged +$3.55 vs six-month band → founding CTA). Progressive enhancement: panels visible/stacked without JS; stepper adds aria-current rail, aria-live, focus moves. No uploads/inputs/network structurally. Registered: hreflang (stamper in-sync), sitemap 1204 URLs. Audit fixes folded in: /security/ prose said "nine" while rendering TEN claims (gate counts 10) — now "ten/diez" everywhere incl. homepage pane 02; `es/admin/submissions` had no `</footer>` (sync skipped it) — closed.

## Decisions a successor must not re-litigate
- **Bolder everywhere, all four surfaces** — founder-approved; executed.
- **Receipts frame owns the homepage** — founder approved verbatim.
- **Demo policy**: own-invoice demo CLOSED until founder reopens; the mockup demo at /ledger/demo/ is the promotable artifact. If the real demo reopens, retarget the founding-band + ledger-hero demo links.
- **Honest urgency only**: founding rate ends at launch + weeks-out fact. NO countdown clocks, spot caps, scarcity theater — voice contract + fact gate govern.
- **Design grammar** (see ADR-002): elevation scale, 3px ink top frames, Fraunces tabular number material, muntin motif (ink-only, disciplined), one dark moment per page, transitions-only motion (no new @keyframes; PRM-gated with static fallback).

## Founder-gated / deferred (in priority order)
1. **$25 list price** — publish at ledger.muntin.digital + register claim, THEN the "$19 vs $25, locked" line ships (pricing-consistency gate in product repo must know it).
2. **"Toward November 13" changelog cadence** (2×/month, GREEN-row facts only from product runbook §2) + Q3 Library Letter.
3. **Founding drip starter set** — `Muntin-Invoice-Decoder/docs/email-drip/ledger/` does not exist yet (runbook §8.6): welcome, monthly progress template, T-7 lock-in.
4. **OG cards refresh** (brand/og pipeline) — critic ranked it highest-reach; launch OG variant + T-7 banner via `inject-batch-banner.mjs`.
5. Cost Index card sparkline via `build-tools-index.mjs` template (numeral-free, from `cost-index-history.json`); H2 muntin waypoints (shell touches ~1,100 pages — needs its own verification budget); pull-quote grammar (new audio-parity surface); mobile sticky-bar per-page primary; security claim-11 badge renumbering (data-id cosmetic, gate-checked).

## Harness + verification (reuse verbatim)
Serve: `python3 -m http.server 8799` in repo root. Chromium `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`; playwright-core at `/tmp/pw/node_modules/playwright-core` (npm install playwright-core into /tmp/pw). Verify every change: EN+ES, light/dark (`colorScheme:'dark'`), `reducedMotion:'reduce'`, 1440+390. Gates: `node scripts/check-all.mjs` — baseline 221/239; the 18 fails are regen/idempotency (deploy-green); diff the ✗ set against a saved baseline log, never absolute counts. CSS: edit `assets/site.css` → `build-css-shells.mjs` → `git add assets/` → `minify-css.mjs --in-place` (needs `npm install lightningcss`) → `git checkout -- assets/` (restores staged). The 2 console 404s on the static server are `/api/*` Worker routes — pre-existing. Commit style: `feat(scope): … (EN+ES)` body with reasoning, `Co-Authored-By: Claude <noreply@anthropic.com>`; pull-rebase before push; NO PR unless asked.
