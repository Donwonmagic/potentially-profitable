# Legal, IP & Claim Substantiation

**NOT_READY**

## Thesis
The plan is the most legally-aware artifact this company has produced — Q-002 correctly re-scopes employer consent from publication to INGESTION, blocks Q-001 behind it, and makes "no basis declared by 2026-09-06" a named kill criterion. That is one legal question, well handled, and it is the only one. I grepped `data/queue.json` for the vocabulary of my domain: counsel 0, attorney 0, LLC 0, insurance 0, indemnity 0, liability 0, "engagement letter" 0, "sales tax" 0. Across `ADR-030-one-price-one-cohort-no-billing-code.md` and `ADR-033-the-ninety-days-and-its-falsifiers.md` the same words return 0, while "signed" returns 8 — the plan retained verbatim the framing (`ADR-030:11` "a signed monthly close", `:72` "a dated, signed statement") that the regulatory seat identified as sitting on the Maryland attest/report line, and adopted none of that seat's seven recommendations into the queue. Meanwhile the entity is still three different things in three files (`terms.html:423` "a Maryland LLC", `Muntin-Invoice-Decoder/docs/tos.md:135` Delaware as "home state", `Muntin-Invoice-Decoder/LICENSE:3` "Muntin Digital LLC"), and `Muntin-Invoice-Decoder/docs/legal-counsel-kickoff.md:353` still asks for "confirmation an entity EXISTS" — a checklist item roughly two months open that no queue item touches. The arithmetic makes this structural rather than accidental: 31.75 declared founderHours plus 7.5h of unpriced review overhead is 39.25h against 39h at the 13h/month floor the plan says it sizes to, so there are no hours left for an attorney, a broker, or a filing, and `capacity.floorPlan.dropOrderToReach13h` cannot cut a legal row because none exists. The single most important thing missing is a Phase-0 legal gate of two binary written answers — is the deliverable as worded a "report" under Md. Bus. Occ. & Prof. Title 2, and does the Tacombi employment agreement assign inventions — because both can void the strategy and both are cheaper than any item currently in M1. Second most important: Q-042 requires "three locations signed" and proves it with a receipts JSON, so the plan can close its day-90 revenue item with no contract, no engagement letter, no third-party-reliance disclaimer and no bound E&O policy in existence. And the plan's only answer to the agent-run exception desk is one prose clause in Q-072 ("the exception desk produces RULES and never numbers") against an absolute claim published on 93 storefront HTML files and at `apps/web/lib/copy.ts:3082,3888,4067` — a distinction about model OUTPUT that does not touch a claim about model INPUT.

## Gaps
- **[BLOCKING] No queue item confirms the legal entity, and the plan requires signing customers out of it**
  - `data/queue.json` contains zero occurrences of "LLC", "entity of record", "formation", "registered agent" or "EIN" (only two "entity" hits, both in unrelated prose). Live contradictions untouched by the plan: `/home/user/potentially-profitable/terms.html:423` "Muntin Digital™ is a Maryland LLC", `:483` and `:656` repeat it; `/home/user/Muntin-Invoice-Decoder/docs/tos.md:135` "State of Delaware (Muntin Digital LLC's home state)" with `:137` venue in Delaware federal courts; `/home/user/Muntin-Invoice-Decoder/LICENSE:3` "Copyright (c) 2026 Muntin Digital LLC". `/home/user/Muntin-Invoice-Decoder/docs/legal-counsel-kickoff.md:353` still lists as an open founder to-do: "confirmation an entity EXISTS (if the product is currently operated as a sole proprietorship, counsel should advise on entity formation FIRST — it gates everything)."
  - Cost: Q-042 (`data/queue.json:958`) makes "three locations signed at $600/month, one invoice sent by hand" the day-90 proof. A signature and an invoice must name a party. If no entity exists, the founder is contracting personally and the liability shield he is relying on when he personally signs a statement of inventory valuation is not there; if a Delaware entity exists but transacts in Maryland unqualified, the Delaware venue clause a first customer's counsel reads is the wrong one and may be unenforceable. The fix is an afternoon; the plan gives it zero hours and no item, so it will be discovered by the first customer's bookkeeper rather than by Muntin.
- **[BLOCKING] The plan engages no counsel at all, and the founder budget is 100% consumed at the floor, so there is no room to**
  - `data/queue.json`: "counsel" 0, "attorney" 0, "legal advice" 0. Same for `ADR-030` and `ADR-033`. Summing `founderHours` across all 46 items = 31.75h (M1 14.5 / M2 8.75 / M3 8.5); `capacity.reviewOverheadPerMonth.hours` = 2.5/month = 7.5h more, for 39.25h against 3 × 13h = 39h at the floor the plan declares it sizes against (`capacity.founderHoursPerMonth.planAgainst: "floor"`). `capacity.floorPlan.dropOrderToReach13h` lists four M1 cuts, none legal. The regulatory seat's two Phase-0 questions — `docs/handoff/bones/board-regulatory.md` "Add a Phase 0 legal gate of two written answers and stop the pivot until both are in hand" — appear nowhere in `data/queue.json`.
  - Cost: Both questions are binary and both can void the plan: if a signed statement of inventory valuation is a "report" under Md. Bus. Occ. & Prof. Title 2 [UNVERIFIED — statutory text not confirmed from this container], the central artifact cannot be sold as worded at any price; if the Tacombi employment agreement carries an invention-assignment clause, the IP is not the founder's to sell. Ninety days of founder-hours would be spent building against an answer nobody asked, and the budget arithmetic means adding either consult now requires displacing a named M1 row — which is exactly the decision `floorPlan` exists to force and cannot force for work it does not list.
- **[BLOCKING] The attest-adjacent vocabulary was carried into the decisions of record unchanged, and the pricing ADR is mis-cited twice**
  - `docs/editorial/decisions/ADR-030-one-price-one-cohort-no-billing-code.md:11` "Muntin sells a **signed monthly close**"; `:49` "a $600/location/month signed close"; `:72` "a dated, signed statement of"; `:87` "six or seven signed". `data/queue.json#strategy.verdict` repeats "a dated, signed statement of food cost". "signed" appears 31 times in `data/queue.json`. `docs/handoff/bones/board-regulatory.md` recommends banning "signed, opinion, attest, certify, assurance, audit, workpaper" from the product and sales script and adding two safe-harbour sentences verbatim to every deliverable; no queue item implements it and no gate detects the words. Separately, `data/queue.json:319` and `:958` both attribute the no-billing-code pricing decision to "ADR-027", which is `ADR-027-the-contract-loads-through-the-hook.md`; the pricing decision is ADR-030.
  - Cost: The wording is what the licensing question turns on, which means the wording must be settled BEFORE counsel is asked or the answer is about the wrong artifact — and today it is settled in the wrong direction, in a ratified ADR (Q-019), reproduced into `/close/` copy by Q-072 and into a customer-visible statement by Q-042. Freezing the risky words into a decision of record makes them expensive to change later precisely because ADRs are the thing this company refuses to relitigate. The ADR-027/ADR-030 mis-citation means the queue's own justification for "no billing code" points at a document that does not contain it — a decision of record that cannot be verified by following its citation.
- **[BLOCKING] The agent-run exception desk is answered with one prose clause, against an absolute claim published 93 times**
  - The plan's entire treatment is inside Q-072's `why`: "the plain-language disclosure that the exception desk produces RULES and never numbers." Against that: 93 storefront HTML files contain "language model", 81 of them the chrome string "— your data, no language model"; `/home/user/Muntin-Invoice-Decoder/apps/web/lib/copy.ts:3082` "What your invoice says does not reach a model", `:3888` "Your invoice content never reaches a language model", `:4067` "We do not read your invoices with a language model"; `docs/marketing-claims.md:26` lists it as a verified spine claim; `docs/sub-processors.md:43-44` "The deterministic v4 architecture removed the LLM sub-processor (Anthropic) entirely; no third party processes invoice content", and `:22` states that once paying customers exist any sub-processor addition "restarts the full 30-day clock with no founder override available." `data/queue.json` contains zero occurrences of "Anthropic" and no item touching `sub-processors.md`, the DPA, the ROPA or the DPIA.
  - Cost: "Produces rules, never numbers" is a statement about what the model OUTPUTS; the published claim is about what the model READS. A session authoring a vendor template from an invoice the deterministic engine failed on has read the invoice, so the disclosure does not cure the claim — it restates it in a way that reads as cured. Under `sub-processors.md:22` the desk cannot lawfully start on day one of the first paid month, which is exactly when Q-042 needs it. And the reader most likely to test the claim is the CPA Q-031 recruits as reviewer. For a company whose only asset is that it does not publish unprovable claims, this is the one failure that is not survivable.
- **[MAJOR] There is no contract. Q-042 proves "signed" with a JSON file, and no engagement letter or third-party-reliance limit exists**
  - Q-042 `doneWhen` (`data/queue.json:958`): "docs/handoff/receipts/first-revenue.json records at least 3 signed locations with dates and monthly amounts, at least 1 signed close (period, signedOn, statementUrl), and at least 1 invoice with sentOn and amount." Its `verify` reads that JSON and checks `monthlyUsd===600`. Nothing requires a countersigned agreement to exist. `data/surface-disposition.json#_toBuild` lists eight pages to build — `/close/`, `/close/specimen/`, `/close/limits/`, `/close/apply/`, three cost-index surfaces, one tool — and none is a terms, engagement-letter or client-agreement surface. The only public terms, `terms.html`, is disposition `keep` under rule `R1-audit-file` and describes "a one-person digital studio" (the retired services line); no queue item edits it.
  - Cost: An audit firm never delivers without an engagement letter, and the plan adopts the audit firm's posture with the click-through SaaS paperwork. Missing specifically: the not-an-audit/review/compilation statement, client responsibilities, a limitations section naming the four known biases the plan itself documents (Q-020 purchases leg, Q-021 expected-on-hand rows, Q-022 missing location predicate, Q-024 partial POS denominator), and — highest value — a no-third-party-reliance clause. Without it, a lender, insurer or buyer who relied on a signed inventory figure sits outside the fee cap. The plan fixes the four biases and then hands the number to a stranger with nothing that says who may rely on it.
- **[MAJOR] Q-002 covers the specimen fixtures only; the IP question, the 15-month corpus, and vendor confidentiality are all outside it**
  - Q-002 `doneWhen` requires `docs/legal/specimen-authorization.md` to declare one of three bases and "names the fixture files it covers, including the seven already committed under MIT." Its `why` scopes the trigger to "INGESTION into a service the employee controls." `data/queue.json` contains zero occurrences of "invention", "moonlighting", "non-compete", "work for hire", "Baldor" or "vendor confidentiality", and two of "Tacombi". The 919-invoice corpus that `docs/audits/company-audit-2026-08-07-appendices/lens-upside.md:13` calls the moat is not named in any item; 124 storefront HTML files name Tacombi, including `about/index.html:78` JSON-LD `worksFor`.
  - Cost: Authorization to ingest and publish one month does not retroactively authorize fifteen months of product development on an employer's confidential vendor pricing, and it answers nothing about whether the extraction engine, templates and corpus are the founder's to sell — which is the question an acquirer, an investor or Tacombi's counsel asks first. Negotiated pricing from named distributors is the vendors' confidential information, which the restaurant cannot waive on their behalf. The employment disclosure across 124 public pages means discovery cost for the employer is one search, and publishing the specimen (Q-030) is precisely the act that prompts someone to look.
- **[MODERATE] The open-data licensing gate validates a rights STRING, not provenance — and non-federal series are already staged in the catalog**
  - `scripts/check-open-data-catalog.mjs:36` — a CC0 dataset must merely "state 'public domain' in rights"; `:37` forbids the word Muntin. There is no check that the underlying series originates with a US federal agency. `data/cost-index-sources.json` stages `PSALMUSDM` with `"family":"imf-salmon","type":"imf"` and the note "FRED/IMF Global price of Fish/Salmon"; `PSHRIUSDM`, `PMAIZMTUSDM` and `PSMEAUSDM` are in the same file. `CLAUDE.md` and `ADR-013-gov-data-sources-policy.md:20` declare the lane "US-gov public-domain (redistributable)"; `ADR-015` Decision 2 assigns CC0 to "a deterministic recompute of public-domain U.S. government" data. IMF Primary Commodity Price series hosted on FRED are IMF works, not US-government works [UNVERIFIED — FRED per-series copyright notice and IMF terms not fetched from this container]. `data/queue.json` contains zero occurrences of CC0, CC-BY, FRED or "redistribut".
  - Cost: The company publishes machine-readable license assertions (`llms.txt`, per-file `license` fields, `DataCatalog` JSON-LD) and is about to sell a commercial service that consumes the same feed. A single promotion of a staged IMF series from `verified:false` to live lands third-party-copyrighted values under a CC0 label that the gate will happily pass because the human-typed rights string still says "public domain." That is a false published license claim, from the company whose brand is not publishing unprovable claims — and it is the one honesty failure this plan's own mutation-testing doctrine (Q-060, `audit-gate-teeth.mjs`) was built to catch and here does not.
- **[MODERATE] None of the paperwork that must exist before a hand-invoice goes out is scheduled: E&O, sales/use tax, or product-facing terms**
  - `data/queue.json`: "insur" 0, "E&O" 0, "sales tax" 0, "tax" appears in no item title. `docs/legal-counsel-kickoff.md:150-155` records "the founder will engage a broker (Embroker, Vouch, etc.)" open since roughly 2026-06-09 with no quote on file. ADR-030 removes billing code entirely (`data/queue.json:958` "The invoice is a PDF and an email — no SKU, no checkout, no billing code"), which removes the only tax calculation the company had; the target cohort is deliberately DMV-clustered across three tax jurisdictions. `terms.html:423` — the storefront's only terms — describes a "one-person digital studio" and is marked `keep` in `data/surface-disposition.json` as part of the public audit file a reviewer reads.
  - Cost: Q-042 sends a real invoice for a real dollar amount on day 90 with no bound professional-liability policy, no determination of whether a hand-invoiced monthly close is a taxable data/IT service, a taxable digital product or a non-taxable professional service in MD/DC/VA [UNVERIFIED — SALT treatment not confirmed], and public terms that describe a business line the company retired. Unremitted sales tax is commonly a personal-liability item for a responsible officer regardless of entity form. Separately, the E&O application asks whether the insured issues signed reports on financial information — a question ADR-030's own wording answers yes, which is the cheapest available second opinion on gap 3 and the plan never buys it.

## Additions
- [founder|1h founder (SDAT/DE lookup + the facts) + 0h agent (the gate and the copy reconciliation)] **Entity of record — one confirmed entity, one governing law, one gate that keeps them agreeing** — done when: `docs/legal/entity.json` exists declaring name, type, state of formation, formation date, registered agent, EIN-on-file (boolean), and a `goodStandingVerifiedOn` date; and `scripts/check-entity-consistency.mjs` is wired into check-all and exits 0 — it fails if `terms.html`, `es/terms.html`, the product `docs/tos.md`, `docs/dpa.md`, `docs/privacy-policy.md` or `LICENSE` names any entity or governing-law state other than the one in entity.json.
- [founder|3h founder + two flat-fee consults [cost UNVERIFIED]; displaces Q-006 and Q-010 in the M1 drop order] **Phase 0 legal gate — two written answers, and nothing downstream ships until both are on file** — done when: `docs/legal/counsel-answers.json` records two entries, each with `question` (verbatim text asked), `answeredBy` (named attorney or the Maryland Board of Public Accountancy), `date`, `answer`, and `constrains` — (a) whether a dated monthly statement of inventory valuation and food cost, prepared for a fee by a non-CPA and delivered with a preparer's signature, is the issuance of a report under Md. Bus. Occ. & Prof. Title 2 and whether COMAR 09.24.01.10's safe harbour reaches it; (b) whether the Tacombi employment agreement's invention-assignment, moonlighting and confidentiality clauses reach the extraction engine, the templates and the 919-invoice corpus used for product development. Q-030 and Q-042 gain both as `blockedBy`.
- [agent|0h founder (the wording is a founder call already implied by the counsel answer); agent writes the gate and the migration] **Settle the deliverable's vocabulary before it is spoken, and gate it** — done when: `scripts/check-attest-vocabulary.mjs` is wired into check-all and exits 0: no shipped surface under `/close/**`, no pricing copy, and no `apps/web/lib/copy.ts` value uses "signed", "opinion", "attest", "certify", "assurance", "audit" or "workpaper" of the deliverable unless the page also carries the two disclaimer sentences verbatim; ADR-030 and `data/queue.json#strategy.verdict` are amended to the settled wording by a superseding ADR; and the two ADR-027 citations at `data/queue.json:319,958` are corrected to ADR-030.
- [agent|0.5h founder (the branch is a founder call) + agent for the amendments and both gates] **Resolve the exception desk against the no-language-model claim — pick one branch, prove it with a gate** — done when: Either (i) `docs/sub-processors.md` lists Anthropic with a role description, DPA §6 / ROPA A2 / the DPIA are amended, the 30-day clock is started and dated, and the absolute claim is replaced with a scoped one everywhere it appears (`apps/web/lib/copy.ts:3082,3888,4067` + the 93 storefront HTML files); or (ii) `scripts/check-exception-desk-redaction.mjs` proves no session receives invoice values — only value-stripped layout structure — and the absolute claim stands. `scripts/check-llm-claim-scope.mjs` is wired into check-all, exits 0, and fails if the published claim and the declared branch disagree.
- [external|1h founder + counsel drafting [cost UNVERIFIED]; 0h agent to amend the queue item] **An engagement letter, and Q-042 amended to require one per signed location** — done when: `docs/legal/engagement-letter.md` exists, marked counsel-reviewed with a reviewer and date, containing at minimum: the confirmed entity, scope, an explicit not-an-audit/review/compilation/attest statement, client responsibilities, an itemised limitations section citing Q-020/Q-021/Q-022/Q-024 by file:line, a no-third-party-reliance clause, limitation of liability tied to fees, mutual confidentiality including vendor pricing, term/termination/data disposition, and venue in the state named in entity.json. Q-042's `doneWhen` and `verify` are amended so each entry in `first-revenue.json#locations` carries `engagementLetterSignedOn`.
- [agent|0h founder; agent, one gate extension plus its mutation proof] **Licensing by provenance, not by string — no non-federal series may land under a CC0 label** — done when: Every dataset in the open-data catalog declares a per-series `sourceAgency` and `sourceIsUSFederalWork` boolean traceable to `data/cost-index-sources.json`; `scripts/check-open-data-catalog.mjs` is extended (and its mutation test in `audit-gate-teeth.mjs` updated) so a CC0 dataset containing any series with `type` other than a US federal agency — including the staged `imf` series PSALMUSDM, PSHRIUSDM, PMAIZMTUSDM, PSMEAUSDM — fails, regardless of what the `rights` string says.
