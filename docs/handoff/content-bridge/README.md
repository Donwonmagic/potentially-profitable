# Content-Bridge Article — Handoff Package

**What this is.** The cornerstone library article *"Privacy-forward restaurant
bookkeeping: what a digital ledger should never do with your numbers"* and its
paired Muntin Ledger landing page (`/privacy-forward`). Together they build
AI-citation authority on `muntin.digital` (the InfoSec + Operations & Margin
pillars) and route qualified readers to Muntin Ledger — the §4.5 "content
bridge" from the SEO maximization handoff.

**Status: DRAFT, ready for handoff. Nothing here is live.**

## Who picks up what

| Folder file | Hand to | Contains |
|---|---|---|
| `article-draft.md` + `publisher-brief.md` | **Library publisher** | The full EN article body, the exact build/data wiring, and the three release blockers. |
| `b-landing-brief.md` | **UX/UI specialist** | The OG image to produce and the full design spec for the Muntin Ledger `/privacy-forward` landing page. |

## Release blockers (all must clear before go-live)

1. **ES translation** — `es/library/contabilidad-de-restaurante-con-privacidad/`.
2. **OG image** — a real `/brand/og/...` asset (don't ship a 404 path).
3. **Six-language audio** — canon §12 blocks publish until every language is
   `rendered`. This is the longest-lead item; start it first.

## Why it matters

`muntin.digital` already routes to Muntin Ledger from the nav and from
end-of-article CTAs on seven finance/privacy feeders (shipped). This cornerstone
is the *editorial* centerpiece of that bridge: a Muntin-Desk-bylined,
fully-AEO-structured piece an AI engine can cite, which closes on Muntin Ledger
as the privacy-forward product that embodies the article's own standard. The
Ledger `/privacy-forward` landing links back to this article — the bidirectional
citation is what reinforces the "two properties, one entity" signal to search
and AI-citation engines.

## One content-review flag (for the publisher)

The draft's §5 opens a sentence with *"In ten years on restaurant floors…"*. The
site's registered operator bio uses a different figure (see `/about/#timeline`
and `data/sourced-claims.json`; the services pages say "14 years on the floor").
Reconcile the number to the registered claim **or** recast the sentence
non-numerically before publish, so it clears `check-fabrications` and stays
consistent with the singular operator bio.
